[build-system]
requires = ["setuptools>=62"]
build-backend = "setuptools.build_meta"

[project]
name = "occ_coder"
version = "1.0.1"
dependencies = [
    "dotenv",
    "pandas",
    "numpy",
    "openai==1.58.1",
    "Path",
    "dash",
    "ragas",
    "langdetect",
]